---
phase: 02-firebase-security-rules-rbac
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - test/package.json
  - test/helpers.js
  - test/rules.test.js
autonomous: true
requirements:
  - RBAC-TEST-INFRA
must_haves:
  truths:
    - "Running `firebase emulators:exec --only firestore \"node test/rules.test.js\"` from the project root completes without a process crash"
    - "All 35+ test cases defined in the matrix execute (some may fail until rules are patched — that is expected at this wave)"
    - "test/helpers.js seeds allowed_users with viewer, editor, and admin members via testEnv.withSecurityRulesDisabled()"
  artifacts:
    - path: "test/package.json"
      provides: "@firebase/rules-unit-testing dependency declaration"
      contains: "@firebase/rules-unit-testing"
    - path: "test/helpers.js"
      provides: "Shared fixture functions for seeding Firestore data in tests"
      exports: ["createTestEnv", "seedAllowedUsers", "seedDocument"]
    - path: "test/rules.test.js"
      provides: "Full rules test suite covering all 35+ role/operation combinations"
      min_lines: 200
  key_links:
    - from: "test/rules.test.js"
      to: "cloudstore_rules"
      via: "fs.readFileSync('cloudstore_rules', 'utf8') passed to initializeTestEnvironment"
      pattern: "readFileSync.*cloudstore_rules"
    - from: "test/rules.test.js"
      to: "test/helpers.js"
      via: "require('./helpers')"
      pattern: "require\\('./helpers'\\)"
---

<objective>
Create the full Firestore rules test infrastructure using Firebase Emulator Suite and `@firebase/rules-unit-testing`. This is the Wave 0 prerequisite — test files must exist and the emulator must run before any rule changes are made.

Purpose: Establish automated verification for all 35+ role/operation combinations so that rule changes (Wave 1) are immediately testable and regressions are caught before deploy.
Output: `test/package.json`, `test/helpers.js`, `test/rules.test.js`
</objective>

<execution_context>
@C:/Users/eugen/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/eugen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-firebase-security-rules-rbac/02-CONTEXT.md
@.planning/phases/02-firebase-security-rules-rbac/02-RESEARCH.md
@.planning/phases/02-firebase-security-rules-rbac/02-VALIDATION.md
@cloudstore_rules

<interfaces>
<!-- Key contracts the test suite must implement against. -->
<!-- Extracted from cloudstore_rules (repo root). -->

Firebase project ID: gspp-9e089
Rules file path (relative to project root): cloudstore_rules
Emulator host: 127.0.0.1, port: 8080

Existing helper functions in cloudstore_rules (DO NOT modify these):
  isInGroup(groupId)     — checks allowed_users/{groupId}.data.members[email] != null
  hasEditorRights(groupId) — getUserRole in ['editor', 'admin']
  hasAdminRights(groupId)  — getUserRole == 'admin'
  getUserRole(groupId)   — reads memberValue from allowed_users, returns 'none'/'viewer'/'editor'/'admin'

allowed_users document shape:
  /allowed_users/{groupId}
  { members: { "email@example.com": { role: "viewer"|"editor"|"admin" } } }
  OR legacy shape: { members: { "email@example.com": "viewer" } }
  (getUserRole handles both — check rules line 152-153)

Test framework API (@firebase/rules-unit-testing v2):
  initializeTestEnvironment({ projectId, firestore: { rules, host, port } })
  testEnv.authenticatedContext(uid, { email })
  testEnv.unauthenticatedContext()
  testEnv.withSecurityRulesDisabled(async (ctx) => { await ctx.firestore().doc(path).set(data) })
  assertSucceeds(promise)
  assertFails(promise)
  testEnv.cleanup()
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test/package.json and install @firebase/rules-unit-testing</name>
  <files>test/package.json</files>
  <action>
Create `test/package.json` in the project root's `test/` directory (NOT in `functions/`). The test directory already exists with Flutter Dart tests — the Node.js rules tests will coexist but use a separate package.json:

```json
{
  "name": "gspp-firestore-rules-tests",
  "version": "1.0.0",
  "description": "Firestore security rules unit tests",
  "private": true,
  "scripts": {
    "test": "firebase emulators:exec --only firestore \"node rules.test.js\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.0"
  },
  "engines": {
    "node": "22"
  }
}
```

Then run `npm install` inside the `test/` directory to generate `test/node_modules/` and `test/package-lock.json`.

Run from project root:
```bash
cd test && npm install
```

The `@firebase/rules-unit-testing` package (v3) is compatible with Node.js 22 and the `@firebase/rules-unit-testing` v2 API used in the test patterns from RESEARCH.md — the API surface (`initializeTestEnvironment`, `assertFails`, `assertSucceeds`) is the same in v3.
  </action>
  <verify>
    <automated>ls "C:/Users/eugen/Desktop/Working Files/GSPP/Application/flutter_application_1/test/node_modules/@firebase/rules-unit-testing" 2>/dev/null && echo "installed" || echo "MISSING"</automated>
  </verify>
  <done>`test/node_modules/@firebase/rules-unit-testing` directory exists</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create test/helpers.js — shared fixtures</name>
  <files>test/helpers.js</files>
  <behavior>
    - createTestEnv() returns a testEnv initialized with projectId 'gspp-9e089' and reads cloudstore_rules from '../cloudstore_rules'
    - seedAllowedUsers(testEnv, groupId) seeds /allowed_users/{groupId} with members: viewer (role:'viewer'), editor (role:'editor'), admin (role:'admin') using the object shape { role: 'viewer' } (not legacy string shape)
    - seedDocument(testEnv, path, data) sets arbitrary document data with rules disabled
    - getViewerCtx(testEnv), getEditorCtx(testEnv), getAdminCtx(testEnv), getUnauthCtx(testEnv) return the correct authenticated/unauthenticated contexts
    - All viewer/editor/admin UIDs and emails are constants exported from helpers: VIEWER_UID, VIEWER_EMAIL, EDITOR_UID, EDITOR_EMAIL, ADMIN_UID, ADMIN_EMAIL, GROUP_ID, OTHER_GROUP_ID
  </behavior>
  <action>
Create `test/helpers.js`. Use `require('@firebase/rules-unit-testing')` and `require('fs')` and `require('path')`.

The path to `cloudstore_rules` is `path.resolve(__dirname, '..', 'cloudstore_rules')` — one level up from `test/`.

Constants to export:
- GROUP_ID = 'groupA'
- OTHER_GROUP_ID = 'groupB'
- VIEWER_UID = 'viewer-uid', VIEWER_EMAIL = 'viewer@example.com'
- EDITOR_UID = 'editor-uid', EDITOR_EMAIL = 'editor@example.com'
- ADMIN_UID = 'admin-uid', ADMIN_EMAIL = 'admin@example.com'

`seedAllowedUsers(testEnv, groupId)` must use `testEnv.withSecurityRulesDisabled()` to write:
```
/allowed_users/groupA = {
  members: {
    'viewer@example.com': { role: 'viewer' },
    'editor@example.com': { role: 'editor' },
    'admin@example.com': { role: 'admin' }
  }
}
```

`createTestEnv()` must set emulator host/port: `{ host: '127.0.0.1', port: 8080 }`.

Export all constants and functions as named exports via `module.exports`.
  </action>
  <verify>
    <automated>node -e "const h = require('./test/helpers.js'); console.log(typeof h.createTestEnv, h.GROUP_ID)" 2>/dev/null || echo "syntax error or missing module"</automated>
  </verify>
  <done>`node -e "require('./test/helpers.js')"` exits without error; `GROUP_ID` is 'groupA', `createTestEnv` is a function</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create test/rules.test.js — full 35+ case test suite</name>
  <files>test/rules.test.js</files>
  <behavior>
    - Uses Node.js built-in test runner (`const { test, before, after } = require('node:test')`) — no external test framework
    - `before` calls createTestEnv() and seedAllowedUsers for GROUP_ID; `after` calls testEnv.cleanup()
    - Each test is named to match the validation matrix (e.g., "viewer reads allowed_users — ALLOW")
    - Every ALLOW case in the matrix uses assertSucceeds(); every DENY case uses assertFails()
    - Tests cover all collections listed in VALIDATION.md test matrix
    - files collection tests cover: in-group read with groupId field (ALLOW), out-of-group read (DENY), no-groupId doc read (DENY), write by editor (ALLOW), write by viewer (DENY)
    - Test for files write uses the PATCHED rule (editor+ write). At Wave 0, the files write test WILL FAIL — that is expected. The test must still exist.
    - lessons update tests cover: custom fields update for assigned instructor (ALLOW), custom fields for unassigned (DENY), non-custom fields update by viewer (DENY)
    - instructor_absences tests cover: create own vacation/sick_leave with status=pending (ALLOW), create admin-type (DENY for viewer), cancel own pending (ALLOW), arbitrary field update (DENY for viewer)
  </behavior>
  <action>
Create `test/rules.test.js`. Use `require('node:test')` and `require('node:assert')` — Node.js 22 has these built-in. Import from helpers.

File structure:
1. Imports + constants from helpers
2. `before()` hook: createTestEnv, seedAllowedUsers(testEnv, GROUP_ID), seedAllowedUsers(testEnv, OTHER_GROUP_ID)
3. `after()` hook: testEnv.cleanup()
4. Test groups by collection (comment separators for readability)

For collections that need pre-seeded documents (materials, tools, lessons, instructor_absences, groups, files), seed them in the `before()` hook using `seedDocument()` from helpers.

Seed data for before() hook (use GROUP_ID = 'groupA'):
- `materials/groupA/items/m1`: { name: 'material1', groupId: 'groupA' }
- `tools_by_group/groupA/items/t1`: { name: 'tool1', groupId: 'groupA' }
- `lessons/groupA/items/l1`: { title: 'lesson1', groupId: 'groupA', instructorIds: ['viewer-uid', 'viewer@example.com'], customFieldValues: {}, createdBy: 'editor-uid' }
- `instructor_absences/groupA/items/a1`: { instructorId: 'viewer-uid', creationType: 'user_request', status: 'pending', type: 'vacation' }
- `groups/groupA`: { name: 'Group A', groupId: 'groupA' }
- `groups/groupA/templates/tmpl1`: { groupId: 'groupA', name: 'template1' }
- `groups/groupA/report_templates/rt1`: { groupId: 'groupA', name: 'report1' }
- `groups/groupA/autocomplete_data/locations`: { items: ['loc1'] }
- `groups/groupA/notifications/n1`: { groupId: 'groupA', title: 'notif1' }
- `drive_catalog_by_group/groupA`: { driveId: 'abc' }
- `users/viewer-uid`: { email: 'viewer@example.com', name: 'Viewer' }
- `users/editor-uid`: { email: 'editor@example.com', name: 'Editor' }
- `files/f1`: { groupId: 'groupA', name: 'file1' }
- `files/f2`: { name: 'legacy-no-groupId' }  (intentionally missing groupId)

Run command from the project root (not from test/):
`firebase emulators:exec --only firestore "cd test && node rules.test.js"`

OR run node from within the test directory:
`firebase emulators:exec --only firestore "node test/rules.test.js"`

The test file should resolve modules relative to itself using `require.resolve` or by having node_modules in `test/`.

Add a shebang-free `require` call that works when run as `node test/rules.test.js` from project root. Ensure `require('@firebase/rules-unit-testing')` resolves by checking if `test/node_modules` is on the path. Add this at the top:
```javascript
// Resolve modules from test/node_modules when run from project root
require('module').Module._nodeModulePaths = (() => {
  const orig = require('module').Module._nodeModulePaths;
  return function(from) {
    const paths = orig.call(this, from);
    const testDir = require('path').join(__dirname, 'node_modules');
    if (!paths.includes(testDir)) paths.unshift(testDir);
    return paths;
  };
})();
```

Actually, simpler: just run the test from inside the test directory. The VALIDATION.md command is:
`firebase emulators:exec --only firestore "node test/rules.test.js"`

This runs node from project root with CWD=project root. For module resolution, ensure the `require('@firebase/rules-unit-testing')` resolves by setting NODE_PATH or by running node from the test directory. Use this pattern instead:

At the top of `rules.test.js`:
```javascript
process.chdir(__dirname); // ensure relative requires work
```

This is the simplest solution — node's module resolution walks up from CWD, and `__dirname` is `test/`.
  </action>
  <verify>
    <automated>cd "C:/Users/eugen/Desktop/Working Files/GSPP/Application/flutter_application_1" && firebase emulators:exec --only firestore "node test/rules.test.js" 2>&1 | tail -20</automated>
  </verify>
  <done>The emulator starts, all tests run (some expected to fail on files write — that is acceptable at Wave 0), process exits without a crash. At minimum: the test file loads, connects to emulator, and produces test output.</done>
</task>

</tasks>

<verification>
Run from project root:
```bash
firebase emulators:exec --only firestore "node test/rules.test.js"
```

Expected outcome at Wave 0:
- Emulator starts successfully
- All test cases execute (no crash / no module-not-found errors)
- ALLOW tests for collections with correct existing rules pass
- files write test and files read restriction test may fail — expected until Wave 1 patches the rules
- Process exits (non-zero is acceptable if only the expected failing tests fail)
</verification>

<success_criteria>
- `test/package.json` exists with `@firebase/rules-unit-testing` in devDependencies
- `test/node_modules/@firebase/rules-unit-testing` exists (npm install completed)
- `test/helpers.js` exports `createTestEnv`, `seedAllowedUsers`, `seedDocument`, all UID/email constants, context getter functions
- `test/rules.test.js` covers all 35+ role/operation combinations from VALIDATION.md test matrix
- `firebase emulators:exec --only firestore "node test/rules.test.js"` runs without crash
</success_criteria>

<output>
After completion, create `.planning/phases/02-firebase-security-rules-rbac/02-01-SUMMARY.md`
</output>
